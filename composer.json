{
    "require": {
        "vlucas/phpdotenv": "^5.6",
        "elasticsearch/elasticsearch": "^8.15"
    },
    "config": {
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}
